//! This module defines a bunch of functions that are used by the runtime. All the functions are
//! used by the built-in data types.

use crate::{CallScope, Result, Value};

pub fn call(_scope: CallScope<'_>) -> Result<Value> {
    todo!()
}
